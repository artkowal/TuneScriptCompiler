# TuneScript Studio - Web Application

[![Python](https://img.shields.io/badge/Python-3.8%2B-3776AB.svg?style=for-the-badge&logo=python&logoColor=white)](https://www.python.org/)
[![ANTLR4](https://img.shields.io/badge/ANTLR-4-E23B2A.svg?style=for-the-badge)](https://www.antlr.org/)
[![Gemini Pro 3.1](https://img.shields.io/badge/Built_with-Gemini_Pro_3.1-8E75B2.svg?style=for-the-badge&logo=google&logoColor=white)](https://gemini.google.com/)

This branch (`web-application`) contains the fully interactive Web IDE implementation of the TuneScript compiler. By leveraging **Pyodide (WebAssembly)**, the entire Python compiler engine runs directly inside the browser. Users can write, compile, and listen to their algorithmic music in real-time without installing any local dependencies.

📖 **Full Documentation & Grammar**: For a comprehensive overview of the language syntax, ANTLR4 grammar rules, and project architecture, please refer to the [Documentation-TuneScript.pdf](Documentation-TuneScript.pdf) included in this repository.

## Academic Context & Credits

This project was developed in collaboration with **[Jakub](https://github.com/Dzejkop02)** as a student project for the **"Formal Languages and Compilers II"** (*Języki formalne i kompilatory II*) course. It was created during the 7th semester of a Bachelor of Science (B.Sc.) degree in Computer Science, specializing in Software Engineering.

🤖 **AI Collaboration**: The modern, responsive frontend interface (`index.html`) was designed and implemented with the assistance of **Google Gemini AI**. This collaboration allowed us to wrap the core compiler logic into a sleek, dark-themed web application, ready to be showcased and hosted natively on GitHub Pages.

## Project Overview

**TuneScript** is a custom Domain-Specific Language (DSL) designed for algorithmic music composition. The compiler processes source code written in `.tune` files and performs a series of transformations to generate standard multi-track MIDI (`.mid`) files. 

The compilation process includes:
1. **Lexical and Syntactic Analysis**: Powered by ANTLR4.
2. **Abstract Syntax Tree (AST) Construction**: Building a structural representation of the source code.
3. **Semantic Analysis**: Verifying the logic, scopes, tempo, and time signatures.
4. **Intermediate Representation (IR) Generation**: Translating the validated AST into an internal representation.
5. **Code Generation (Backend)**: Encoding the IR instructions into a binary MIDI format.
6. **Web Integration**: The generated binary data is immediately injected into the Magenta `<midi-player>` for in-browser playback and download.

## Repository Structure

* `grammar/` - ANTLR4 grammar definitions for the TuneScript language (`.g4` extension).
* `generated/` - Python lexer, parser, and visitor components automatically generated by ANTLR4.
* `src/` - The core compiler engine, containing:
  * `ast_builder.py` / `ast_nodes.py` - AST node definitions and the tree construction logic.
  * `semantic_analyzer.py` - Semantic validation algorithms.
  * `ir_generator.py` - Transformation logic from AST to Intermediate Representation (IR).
  * `midi_generator.py` - The backend module utilizing the `mido` library to generate binary MIDI files.
* `examples/` - Sample TuneScript source code files.
* `output/` - The destination directory for the compiled `.mid` files.
* `main.py` - The primary entry point for the CLI application.
* `Documentation-TuneScript.pdf` - Comprehensive project documentation.
* `index.html` - The main Web IDE interface (HTML, CSS, JavaScript) running Pyodide.

## Running Locally (WEB)

To run the Web IDE locally on your machine, you do **not** need to install the Python libraries manually, as Pyodide fetches them dynamically via WebAssembly. You only need a basic local HTTP server.

1. **Clone the repository and switch to the web branch:**
```bash
git clone https://github.com/artkowal/TuneScriptCompiler.git
cd TuneScriptCompiler
git checkout web-application
```

2. **Start a local HTTP Server:** Due to CORS security policies, the browser needs a server to fetch the internal `.py` files. Run the built-in Python server in the root directory of the project:
```bash
python -m http.server 8000
```

3. **Open the Application:** Open your web browser and navigate to:
```bash
http://localhost:8000
```

## Prerequisites and Installation (CLI)

The software requires **Python 3.8** or higher.

1. **Clone the repository and switch to the CLI branch:**
```bash
git clone https://github.com/artkowal/TuneScriptCompiler.git
cd TuneScriptCompiler
git checkout base-compiler-CLI
```
   
2. Initialize a virtual environment (Recommended):

macOS/Linux:

```bash
python3 -m venv .venv
source .venv/bin/activate
```
Windows:

```bash
python -m venv .venv
.venv\Scripts\activate
```

3. Install dependencies:
Install the required libraries (including mido and the ANTLR4 runtime) via the provided requirements file:

```bash
pip install -r requirements.txt
```
## Usage (CLI)
To compile a TuneScript source file, execute the `main.py` script and pass the path to the `.tune` file as an argument.

Example command:

```bash
python main.py examples/example.tune
```
Upon successful compilation, the standard output will display the execution logs of the subsequent compilation phases, and the resulting `.mid` file will be saved in the `output/` directory.